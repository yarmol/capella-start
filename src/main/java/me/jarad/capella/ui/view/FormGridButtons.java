package me.jarad.capella.ui.view;import com.google.common.collect.Lists;import com.vaadin.server.FontAwesome;import com.vaadin.ui.Button;/** * @author vyarmole on 10.04.17. */public class FormGridButtons { // private List<String> buttons;  public FormGridButtons() {}  public static Button[] getGridButtons() {    Button addNewBtn = new Button(FontAwesome.PLUS_CIRCLE);    Button deleteBtn = new Button(FontAwesome.MINUS_CIRCLE);    Button[] buttons = Lists.newArrayList(addNewBtn, deleteBtn).toArray(new Button[2]);    /*addNewBtn.addClickListener(e -> {      showItem();    });    deleteBtn.addClickListener(e -> {      Set<User> selectedItems = grid.getSelectedItems();      if (selectedItems != null && !selectedItems.isEmpty()) {        User item = selectedItems.iterator().next();        userService.removeUser(item.getId());      }    }); */    return buttons;  }}